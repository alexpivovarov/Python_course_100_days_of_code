creating user interface (window, canvas, buttons, entries), aligning them properly, placing image

adding dialog boxes and pop-ups for better user experience

opening files and appending data into it from entries

clearing entries by deleting information
